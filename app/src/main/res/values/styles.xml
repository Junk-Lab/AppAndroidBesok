<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal do aplicativo -->
    <style name="Theme.AppBurleService" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Propriedades do tema principal -->
    </style>

    <!-- Tema da Splash Screen -->
    <style name="Theme.AppBurleService.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>